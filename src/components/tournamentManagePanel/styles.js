import {StyleSheet} from 'aphrodite';

const styles = {
  container:{
      position:'absolute',
      background:'rgb(180, 144, 90)',
      width:'90%',
      marginLeft:'5%',
      padding:'10px',
      minHeight:'200px',
  },
};

const resp = StyleSheet.create({

});

export {resp, styles};
